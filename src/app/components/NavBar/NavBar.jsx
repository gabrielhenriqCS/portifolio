import "./navbar.css"

export default function NavBar() {
    return (
        <div>
            <nav className="navbar">
                <a href="/">Página Inicial</a>
                <a href="/projetos">Projetos</a>
            </nav>
        </div>
    )
}